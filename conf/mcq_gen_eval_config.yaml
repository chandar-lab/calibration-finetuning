defaults:
  - model: qwen3_1p7b
  - mcq_gen: medical_uniform
  - eval_target: mcq_gen_baseline
  - inference: hf
  - _self_

seed: 11
output_root: ${mcq_gen.output_root}

hydra:
  run:
    dir: ${output_root}/${now:%Y%m%d_%H%M%S}_${eval_target.name}_mcq_gen
  job:
    chdir: false
