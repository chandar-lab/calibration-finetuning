name: calibrate_sft
eval:
  enabled: true
  every_n_steps: 10
  output_subdir: eval
  sample_eval:
    enabled: true
    protocol: independent
    num_samples: 8
    batch_size: 16
    seeds:
      - 11
    max_prompts_per_split: 1
  logit_eval:
    enabled: true
    num_mc_samples: 2
    batch_size: 16
    max_prompts_per_split: 1
sample_eval:
  enabled: true
  protocol: independent
  num_samples: 64
  batch_size: 16
  seeds:
    - 11
  max_prompts_per_split: 8
logit_eval:
  enabled: true
  num_mc_samples: 4
  batch_size: 16
  max_prompts_per_split: 8
